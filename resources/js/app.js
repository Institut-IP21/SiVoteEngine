// Alpine.js is provided by Livewire 3 (it bundles and starts Alpine v3 via
// @livewireScripts, which every page loads through layouts/main).
// Do NOT import a standalone Alpine here — doing so loads a second Alpine
// instance and breaks all x-data directives. The only Alpine usage in this
// app (a password show/hide toggle) is Alpine-v3 compatible.
